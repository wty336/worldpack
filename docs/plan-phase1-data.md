# Phase 1 · 五侧信道训练数据构造与评测计划

> 上游：`docs/plan-local-14b.md` §6（Phase 1 窄模块蒸馏）、§10.4（补标预算纪律）；
> 处置清单见 `docs/local14b-p012-retro.md` §8.3/§10。
> 本计划回答三个问题：**训什么、训多少**（§1）、**数据从哪来**（§3-§4）、
> **怎么证明训了有用且没窄化**（§5-§7）。
>
> 核心主张（与最初的"五个都造"方案的两处修正）：
> 1. **先造尺子，再造数据** —— 当前评测尺子每包每类仅 6 条（1 条 = 16.7pp），
>    训前训后的差异不可分辨。2026-09-11 的"flash dedup 40-50%"错误对照线就是坏尺子的产物。
> 2. **dedup 不进训练集** —— 实测基座与 flash 双方已 100%（`reports/sidechannel-budget-retest-20260911.md`），
>    无缺口可补；它只作为**冻结回归护栏**存在。

---

## 1. 决策表：训什么、训多少

| 模块 | 实测缺口（2026-09-11/12 重测后） | 进训练集 | 训练量 | 评测集目标 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| **extract** | railed 轨道事实 **0/3**（flash 3/3）；根因 = 事实提取层未入桶 | ✅ | **600~1500** | 每包 30~50 条期望事实 | **P0 必训** |
| **judge** | 拦截 83~94% vs flash 100%（ancient setting 类 4/6 拖累）；误报 0% 已达标 | ✅ | **500~1200** | 每包每类 ≥20（对抗 ≥60、正常 ≥40） | **P0 小补强** |
| **compress** | 保全率 0.23~0.67 vs flash 0.47~0.89（长输入最弱 0.27） | ✅ | **200~500** | 20~30 样本（规则保全） | P1 要训 |
| **reflect** | 14B 仅 1/3 产出合法洞察（2/3 诚实输出「无」） | ✅（可砍） | **200~400** | 30+ 样本 | P2 最低 |
| **dedup** | **双方 10/10 = 100%（无缺口）** | ❌ **只造评测集** | 0 | 100~200 对（含易混淆） | 回归护栏 |

**为什么 dedup 不训**：单 LoRA 多任务下，dedup 是全任务中最简单的一个（输入 <1K、输出 2 字），
梯度信噪比最低，参与训练会稀释其他任务的容量并可能把模型往"极短输出"方向带；
而它作为护栏的价值（防回退）远大于作为训练样本。

---

## 2. Step 0 · 提示词去域化（半天，零成本，必须最先做）

**证据（代码级）**：`game_agent/memory.py:43-52` 的 `EXTRACT_SYSTEM` 把事实类型写成
「身份、来历、名字、**剑名、师承**、喜好、承诺、约定、托付等」——**引擎提示词本身把题材写死成武侠**。
不改它就开始造数据，训出来的是"武侠事实提炼器"，换题材即残。

已核对：`REFLECT_SYSTEM`（`memory.py:56-62`）、`COMPRESS_SYSTEM`（`compression.py:15-25`）、
`JUDGE_SYSTEM`（`judge.py:13-20`）均为题材中性 ✅ —— **本步只改 extract**。

改法（保持与 parser 的契约不动：不编号、不解释、≤5 条、无则「无」、不重复已有事实）：

```
身份身世 / 称谓与别名 / 所属与来历 / 物品与装备 / 能力与技艺 / 地点与势力 /
承诺与约定 / 债务与人情 / 目标与线索 / 关系变化
```

- 每类给 1 个**跨题材**示例（古风 / 都市 / 科幻各一），避免新枚举又偏向单一题材；
- 验收：新增跨题材样例测试（4 个不同题材包 × 3 条输入，断言抽出的事实类型覆盖枚举且不越界）；
- 纪律：提示词改动 = 新实验（`plan-local-14b` §9），需 bump `prompt_version` 并在数据/报告里记录。

---

## 3. Step 1 · 评测集扩域与冻结（1.5~2 天）

### 3.1 现状：尺子太短（实测）

| 评测器 | 现状规模 | 单条分辨率 | 目标规模 |
| --- | --- | --- | --- |
| `scripts/judge_sensitivity.py` | 30 条/包（对抗 6/类 + 正常 12） | **16.7pp** | 每类 ≥20（对抗 60+/包、正常 40+） |
| `scripts/dedup_test.py` | 10 对 | 10pp | 100~200 对（含易混淆负例） |
| `scripts/railed_fact_check.py` | 3 条轨道事实 | **33pp** | 30~50 条 |
| `scripts/memory_regression.py` | 121 回合 + 期望事实集 | — | 保留 + 扩期望事实 |
| `scripts/reflect_smoke.py` | 3 样本 | 33pp | 30+ 样本 |
| `scripts/phase1_probe.py`（compress） | 5 样本 | 20pp | 20~30 样本 |

### 3.2 泛化包：直接检验"窄化"的仪器

现有 4 个有语料的包**轴值高度同质**（都是中文第三人称、实体类型相近、数值系统只是换名、
关系动力都是好感度单向累积）——**包级留出测不出窄化**（剔掉 urban_neon，留下的是同分布的另一份样本）。
因此除 `urban_neon` 剔除（`plan-local-14b` §7.3 既有纪律）外，新增两个**轴值留出包**：

| 代号 | 造法 | 留出的轴 | 用途 |
| --- | --- | --- | --- |
| **G1 真实风味包** | `scripts/import_story.py --offline` 从新素材生成（题材与训练集不同，如民国谍战 / 蒸汽朋克） | 题材 + 语体 + 实体类型 | 检"真实脏包"泛化 |
| **G2 轴值留出包** | 手工/半自动合成 | 书信体 + 非中文姓名 + 不同数值系统（信誉/声望）+ 敌对型关系动力 | 检"同题材不同形态"泛化 |

两者都必须产出 `judge_corpus.yaml` + `smoke_profile.yaml`（`import_story.py --with-corpus` 可自动生成草稿，
按门禁报告手工修 3~6 条 ooc 类，参考 `plan-worldpack-qa` §7.6.3）。

### 3.3 冻结纪律

1. 评测集**先提交进 git 并打 tag**（如 `phase1-eval-frozen`），训练开始后不得再改；
2. 训练数据与评测集做**指纹去重**：同一包、同一回合前缀、同一 fact 不得跨集（前缀级去重，非样本级）；
3. 训练报告必须记录 `eval_set_sha256`；评测报告必须记录 `budget_policy` + `prompt_version`；
4. 评测集**不得**包含任何训练数据的生成种子（合成器 seed 与评测器 seed 隔离）。

---

## 4. Step 2 · 数据构造（3~4 天）

### 4.1 三源与配比

| 来源 | 占比 | 优势 | 坑与对策 |
| --- | --- | --- | --- |
| **YAML 程序合成** | 主干 60% | 零 API 成本、标签精确（程序推导）、**天然可扩域**（可造未见题材/结构的包） | 语体偏干净、结构易单调 → 变化节点/NPC/数值规模，见 §4.4 |
| **真实轨迹切窗** | 锚 20~30% | 生产分布真实（29K 上下文、压缩摘要、状态栏噪声） | **只覆盖现有包 → 窄化主因**，限量；按包留出后仅剩 3 包可用 |
| **flash 补标** | 10~20% | 唯一能产出真实叙事流的场景（compress/reflect/extract 需要） | 预算纪律 + **截断样本不得入库**（`complete_checked` 的 `truncated`） |

### 4.2 五模块逐一：输入形态契约 + 标签 + 生成器

> **输入模板必须逐字对齐生产调用**（下面模板引自代码），否则训练分布与推理分布不一致。

#### 4.2.1 extract（P0，600~1500 对）

- **输入**（`game.py` `_extract_facts`）：
  - system = `EXTRACT_SYSTEM`（Step 0 去域化后）
  - user = `已有事实：{existing}\n\n<回合内容>\n{recent_8_turns}\n</回合内容>`
- **标签**：`重要性|事实` 行（1~10），≤5 条；无值得记的 → `无`；不得重复「已有事实」
- **生成器**：
  1. **程序化（主干）**：pack YAML 的 lore / npc 卡 / 事件效果 / 数值系统 → 生成"回合内容"
     （埋入已知的专名、数字、承诺、约定），标签 = 期望事实集（**精确**，可机器验证）；
  2. **轨迹切窗**：真实 recent 文本 + flash 补标（temp=0），抽检 5~10%；
  3. **负例（必配 ≥15%）**：纯日常寒暄回合 → 标签 `无`；已有事实的近义改写 → 亦为 `无`（考去重纪律）
- **覆盖要求**：10 类事实 × ≥3 题材，**数值类与承诺类各 ≥15%**（C 轴核心）

#### 4.2.2 judge（P0，500~1200 条）

- **输入**（`judge.py` `_judge_messages`）：
  - system = `JUDGE_SYSTEM`
  - user = `<材料>\n{materials}\n</材料>\n\n<最新叙事>\n{narration}\n</最新叙事>`
  - 材料 = `ContextBuilder.status_text(state, node)`（场景 / 在场 NPC / 好感 / 事实 / 记忆）
- **标签**：`通过` 或 `问题类型：描述`（≤2 条，引用叙事原文）
- **生成器（对抗样本标签可程序推导）**：
  - **setting（程序化）**：从材料里取一个事实（剑名/日期/地点/能力/身份），在叙事里改成矛盾值；
  - **confab（程序化）**：叙事中声称一个材料里**不存在**的承诺/约定/事件；
  - **OOC（半程序化）**：**必须从物化后的角色卡** `boundaries/speech_style` 出发改写语气或底线
    （star_ring 的教训：note 引用生成时的卡、物化后卡已变 → 漏判，见 `plan-worldpack-qa` §7.6.3）；
  - **normal（真实）**：真实轨迹叙事 + 对应材料，标签 `通过`；
  - **难例集（≥10%）**：从门禁报告 `hit=false` 的对抗样本反向构造同族变体
- **覆盖要求**：三类对抗各 ≥1/3；**setting 类优先补**（实测最弱）；每题材每类 ≥5

#### 4.2.3 compress（P1，200~500 对）

- **输入**（`game.py` `_compress_history`）：
  - system = `COMPRESS_SYSTEM.format(target=800)`
  - user = `<旧摘要>\n{existing}\n</旧摘要>\n\n<新增历史>\n{history_text}\n</新增历史>`
- **标签**：≤800 字 Markdown，保留 ①关系与好感 ②关键选择/承诺/约定（数字、名字、暗号、日期）
  ③重要事件与结果 ④未完成目标与线索 ⑤失败尝试与原因
- **生成器**：
  1. **程序化**：合成历史中埋入 K 个"必保全"要点（数字/专名/暗号/日期/承诺），
     参考摘要由模板生成（含全部 K 点），评测 = 保全率（规则抽取，`phase1_probe.eval_compress` 同款）；
  2. **真实形态**：轨迹首压 + 增量合并，flash 补标（`COMPRESS_MAX_TOKENS=4000`，
     **过滤 `truncated=true`**）；
  3. **长输入档 ≥20%**：20K 级输入（实测 14B 最弱 0.27，必须专门补）
- **负例**：含大量日常寒暄的历史 → 摘要**不应**出现寒暄关键词

#### 4.2.4 dedup（**仅评测集**，100~200 对）

- **输入**（`memory.py` `_is_semantic_duplicate`）：
  `<既有事实>\n{similar}\n</既有事实>\n\n<候选事实>\n{fact}\n</候选事实>`（bigram 预筛后触发）
- **造法**：同一事实 3 种改写（换词 / 换语序 / 换指代）→ `重复`；
  语义距离分级（远 / 中 / **近但不同**）→ `不重复`；
  含 bigram 边界样本（无重叠 vs 有重叠但不同）——顺带验证预筛不漏判
- **用途**：冻结回归护栏（训前训后都必须 100%，掉了即回退）

#### 4.2.5 reflect（P2，200~400 对）

- **输入**（`game.py` `_reflect_npc`）：
  - system = `REFLECT_SYSTEM`
  - user = `<角色>{name}</角色>\n\n<近期记忆>\n{numbered}\n</近期记忆>`
    （+ 可选 `<已有洞察>`）；素材 = 最近 12 条记忆，门控 ≥8 条
- **标签**：`洞察|来源编号`（编号必须命中素材序号）、≤2 条；信息不足 → `无`
- **生成器**：程序化关系模板（8~12 条记忆隐含"疏远→信任 / 信任→戒备 / 客气→依赖"等趋势），
  标签 = 带**正确编号**的模板洞察；补标档用真实记忆切片（temp=0）
- **负例**：素材不足或互相矛盾的记忆列表 → `无`；**来源编号错配**的样本必须能被判错（列入评测项）

### 4.3 质量门（生成即校验，逐条）

| 检查 | 规则 |
| --- | --- |
| **契约 round-trip** | 标签过对应 parser 必须无损：`parse_facts` / `parse_verdict` / `parse_insights` / dedup 判词 |
| **截断/空/异常** | 补标样本 `truncated=true` 或空 → 丢弃；`无` 占比每模块设上限（防"全输出无"退化） |
| **去重** | 与评测集 + 数据集内部做前缀/相似度去重（复用 `game.py` `_check_repetition` 的 0.6 阈值思路） |
| **配额** | 轴覆盖矩阵（§4.4）每格 ≥N，未达标不出库 |
| **元数据** | 每条记录 `{source, pack, axis_tags, generator, seed, budget_policy}`，便于后续切片分析 |

### 4.4 轴覆盖矩阵（"不窄化"的可检查形式）

| 轴 | 取值（示例） | 训练配额 | 评测留出 |
| --- | --- | --- | --- |
| 时代/题材 | 古代武侠 / 现代都市 / 太空科幻 / 仙侠 | 各 ≥15% | **民国谍战**（G1） |
| 语体 | 口语叙事 / 冷硬白描 / 技术术语 | 各 ≥10% | **书信体**（G2） |
| 实体类型 | 人名 / 地名 / 组织 / 物品 / 技艺 / 货币 / 信誉 | 各 ≥5% | 非中文姓名（G2） |
| 数值系统 | 银两 / 信用点 / 修为 / 声望 | 各 ≥10% | 信誉+声望混合（G2） |
| 关系动力 | 师门 / 雇佣 / 恋爱 / 亲情 | 各 ≥10% | **敌对型**（G2） |
| 违规形态 | OOC / 设定矛盾 / 虚构 | 各 ≥1/3 | 混合形态（G1） |
| 结构规模 | 节点 4~6 / 8~12 / 16+；NPC 1~2 / 3~5 | 覆盖三档 | NPC 6+（G2） |
| 输入形态 | 首压 / 增量合并 / 长输入(20K) / 脏输入 | 见各模块 | 脏输入（G1） |

> 规则：**每个轴至少有一个取值只出现在评测集** —— 这是"窄化"从担心变成可测指标的关键。

---

## 5. Step 3 · 训练（1~2 天）

- **单 LoRA 多任务**（共享底座、一次部署，`plan-local-14b` §6.3）；任务靠 system prompt 区分（已在库）。
- **混比**：`extract : judge : compress : reflect ≈ 4 : 3 : 2 : 1`（**dedup 0**）；
  每个 epoch 跨任务打乱，防任务块状学习。
- **序列长度**：侧信道输入 <10K（compress 可到 20K）→ 4090 QLoRA 可行（`plan-local-14b` D.7）。
- **可复现**：记录 seed / epochs / lr / 数据集 sha256 / `prompt_version`；**不做**主回合蒸馏（Phase 2）。
- **不动的东西**：`game_agent/budgets.py` 的预算常量（评测口径的一部分），如需改动则整套重测。

---

## 6. Step 4 · 评测（1 天）

### 6.1 三方对照协议

| 列 | 含义 | 命令口径 |
| --- | --- | --- |
| 基座 | 未训练 14B（Phase 0 已有初值） | 同命令 / 同 seed / 同 budget_policy |
| 训练后 | LoRA 模型 | 同上 |
| flash | 参照系（教师） | 同上，且不带任何 `DEEPSEEK_*` shell 前缀（§9.5 纪律） |

### 6.2 指标与判据

| 模块 | 指标 | 判据 | 泛化要求 |
| --- | --- | --- | --- |
| extract | 轨道事实保持率 / 期望事实召回 | ≥ flash − 5pp | G1+G2 均测（防"只认训练题材"） |
| judge | 每类拦截率 / 误报率 | 拦截 ≥ `INTERCEPT_MIN=0.80` 且 ≥ flash−5pp；误报 ≤0.10 | 同上；G2 强制 |
| compress | 保全率 / 结构合格率 / 截断率 | 保全率 ≥ flash−10pp；截断率 0 | 长输入档必须包含 |
| reflect | 洞察合法率 / 矛盾率 | 合法率 ≥80%；矛盾率 0 | 来源编号正确性单列 |
| dedup | 拦截率（冻结集） | **必须 100%（回退即失败）** | — |

### 6.3 回归护栏（任何一项失败即判训练有害）

1. **A 轴协议不许退化**：`worldpack_smoke` 两包通关 + 熔断率不升（侧模块与主回合若共底座，威胁真实存在）；
2. **dedup 冻结集必须 100%**；
3. **门禁四连**（`check-worldpack` / `pytest` / `judge_sensitivity` / `worldpack_smoke`）全绿；
4. **契约合规**：输出过 parser 的失败率为 0（`无`/`通过`/`洞察|编号` 等格式）。

### 6.4 报告 schema（新增字段）

```json
{
  "budget_policy": "budgets.py@<sha>", "prompt_version": "<hash>",
  "train_set_sha256": "...", "eval_set_sha256": "...",
  "summary": { "<cat>": {"n": 0, "hits": 0, "rate": 0, "unknown": 0} },
  "baseline": "zero-shot | flash"
}
```

> 记录 `budget_policy` 是 2026-09-11 的教训：`judge_sensitivity_*.json` 没记预算策略，
> 导致"40-50%"是哪个配置时代的产物无法分辨。

### 6.5 决策门 1（落地版）

- **达标** → 进 Phase 3 窄模块落地（`plan-local-14b` §8）；
- **未达标** → 先查数据质量（清洗/标注/配额），再查容量；一轮回炉后仍不达标 → 砍掉该模块的本地化
  （flash 兜底），不阻塞主线。

---

## 7. 里程碑与成本

| Step | 产出 | 时间盒 | API 成本 |
| --- | --- | --- | --- |
| 0 提示词去域化 | `EXTRACT_SYSTEM` 新版 + 跨题材测试 | 0.5 天 | 0 |
| 1 评测集冻结 | 扩域评测集 + G1/G2 泛化包 + tag | 1.5~2 天 | ~¥5（真机校验少量） |
| 2 数据构造 | 合成器 + 数据集 + 质量报告 | 3~4 天 | **¥25~45**（compress 最贵） |
| 3 训练 | LoRA + 训练报告 | 1~2 天 | GPU 电费（4090 可做） |
| 4 评测 | 三方对照 + 泛化 + 护栏报告 | 1 天 | ¥10~20 |

---

## 8. 风险与对策

| 风险 | 表现 | 对策 |
| --- | --- | --- |
| **窄化**（题材/语体/结构） | 泛化包掉点，训练包虚高 | 轴级留出（§4.4）+ 合成扩域 + 轨迹限量 |
| **过度扩域** | 只会处理"模板味"干净文本，真实脏包退化 | 保留真实包锚点（20~30%）+ 合成包结构多样化 |
| **五任务干扰** | 某任务提升、另一任务回退 | 混比设计 + dedup 不进训练 + 每模块单独评测 |
| **评测污染** | 训练集混入评测前缀 → 虚高 | 前缀级指纹去重 + 评测集冻结 + sha256 记录 |
| **尺子口径漂移** | 不同 run 的预算/提示词不可比 | `budget_policy` + `prompt_version` 写进报告 |
| **补标截断污染** | 截断输出被当标准答案 | `complete_checked` 的 `truncated` 过滤；compress 预算 ≥4000 |
| **判官标签与门禁打架** | 训练后 judge 与 `judge_sensitivity` 判据不一致 | 标签定义直接对齐 corpus 的 `category/expected` 语义；契约测试 |

---

## 9. 待拍板（需要决策的问题）

1. **reflect 是否进训练集**？（最低优先级；砍掉可省 ~200-400 条数据成本）
2. **G1/G2 泛化包的题材选择**：G1 用"民国谍战"还是"蒸汽朋克"？G2 的轴值组合是否照 §4.4 表？
3. **侧模块是否与主回合共用底座**？（影响 A 轴回归口径与 Phase 2 的迁移路径）
4. **评测集扩容的人工上限**：程序化生成 + 抽检比例（建议 20%）是否可接受？

---

## 附录 A · 五模块契约速查

| 模块 | 输入模板（生产同款） | 标签契约 | 解析/校验代码 | 预算 |
| --- | --- | --- | --- | --- |
| extract | `已有事实：{...}` + `<回合内容>` | `重要性\|事实` ×≤5，或 `无` | `memory.parse_facts` | `EXTRACT_MAX_TOKENS=500` |
| judge | `<材料>` + `<最新叙事>` | `通过` / `问题类型：描述` | `judge.parse_verdict` | `JUDGE_MAX_TOKENS=500`（空/截断→2000） |
| compress | `<旧摘要>` + `<新增历史>` | ≤800 字 Markdown，5 类信息 | `compression.ensure_pairing` + 目标 `SUMMARY_MAX_TARGET=800` | `COMPRESS_MAX_TOKENS=4000`（→8000） |
| dedup | `<既有事实>` + `<候选事实>` | `重复` / `不重复` | `memory._is_semantic_duplicate` | `DEDUP_MAX_TOKENS=500` |
| reflect | `<角色>` + `<近期记忆>` | `洞察\|编号` ×≤2，或 `无` | `memory.parse_insights` | `REFLECT_MAX_TOKENS=500` |

统一纪律：**空或截断 → 升级重试一次**（`budgets.complete_checked` / `retry_tokens_for`）；
门禁判定的未知轮不进分母（`judge_corpus.majority_hit`）。

## 附录 B · 相关索引

- 上游计划：`docs/plan-local-14b.md` §6/§7/§9/§10.4
- 执行复盘：`docs/local14b-p012-retro.md` §8.3（处置清单）/§10（对 Phase 1 的修正）
- 决策门 0 报告：`reports/local14b-p0-20260911.md`
- 预算修复与重测：`reports/sidechannel-budget-retest-20260911.md`（含四个残留问题处置）
- 世界包生产链：`docs/worldpack-manual.md`、`docs/plan-worldpack-qa.md` §7.6
- 代码接缝：`game_agent/budgets.py`、`game_agent/judge.py`、`game_agent/memory.py`、
  `game_agent/game.py`（`_extract_facts`/`_reflect_npc`/`_compress_history`）、`game_agent/compression.py`
