# M1.5 · 体验基线与债务清理 —— 执行计划

> 背景：M1 全部验收达成（95 测试全绿、两结局真机通关、审计/注入/OOC 通过），但存在五个缺口：长会话基线缺失、真人试玩为零、通关脚本不可复现、时间触发事件未实现、M2 范围过肥。
> M1.5 的目标：**在进入 M2 前补齐基线与小债，并把 M2 拆成可独立验收的三段**。
> 原则：边界集 + 保留集（先立基线再改进）、最小 diff + 可回滚、保持简单。

---

## 0. 范围

### 做

| # | 事项 | 谁做 | 类型 |
| --- | --- | --- | --- |
| W-A | 通关/注入脚本固定 RNG seed，可复现 | 引擎侧 | 离线为主 |
| W-B | 事件系统补"时间触发"机制（`trigger.kind: time`） | 引擎侧 | 全离线 |
| W-C | 自动存档（节点完成时）+ CLI `/new` 重开 + 结局后引导 | 引擎侧 | 离线+小真机 |
| W-D | **长会话基线**：记忆保持回归集，测 40/80/120 轮腐化曲线 | 引擎侧 | 真机（~¥1） |
| W-E | 真人试玩 1–2 局，收集体验问题 | **用户** | 真机 |
| W-F | 根据基线+体验问题，把 M2 拆成 M2a/b/c 并落文档 | 引擎侧 | 文档 |

### 不做（留给 M2）

- 剧情摘要压缩（M2a 核心，等基线数据再动手）；
- NPC 记忆、LLM-Judge 语义层 OOC、多人场景、Web 前端（M2b/M2c）；
- 世界包内容扩展（M1.5 不碰内容，保持现有两条通关路径不变、省 token）。

---

## 1. 工作包明细

### W-A · 可复现性修复

**问题**：`playthrough.py` / `injection_test.py` 用未 seed 的 `random.Random()`，后山奇遇（30% 概率）等日程事件不可复现——违背"边界集+保留集"的可比较原则。

**动作**：
1. 两个脚本加 `--seed` 参数（默认 42），`Game(..., rng=random.Random(seed))`；
2. seed 与事件序列写入存档/报告（`saves/*.json` 的 `run_meta` 字段）；
3. 真机冒烟：同 seed 重跑一小段，确认事件触发序列一致。

**验收**：`--seed 42` 两次运行的事件序列一致（offline 用 stub rng 验证；真机小样本抽查）。

### W-B · 时间触发事件机制

**问题**：design.md §9 三类触发只实现了条件、日程，时间触发（第 N 天必发生）缺失。

**动作**：
1. `EventTrigger.kind` 增加 `"time"`，`when` 只允许 day 条件（worldpack 加载期校验）；
2. `Game.end_day` 中检查并触发时间事件（触发消息注入下一天的首个叙事回合）；
3. 离线测试：时间事件在指定天触发、once 防重、提前不触发、非 day 条件的 time 事件被加载期拒绝。

**验收**：离线测试全绿；不新增世界包内容（现有两条通关路径不受影响）。

### W-C · 自动存档 + 重开引导

**问题**：存档只有手动单槽位；结局后只能读档，没有重开入口。

**动作**：
1. 每个主线节点完成时自动存档到 `saves/autosave.json`（引擎侧钩子，不打扰叙事）；
2. CLI 增加 `/new` 命令（重置 GameState + 清空历史，回到开场）；
3. 结局提示文案改为「/load 读档 · /new 重新开始 · /quit 退出」。

**验收**：离线测试（节点完成 → autosave 存在；/new 后状态为初始值）+ 真人试玩顺手验证。

### W-D · 长会话基线（M2 压缩的边界集）

**问题**：M1 最长 ~40 回合，全量历史塞得下；M2 的 300 轮验收没有任何基线。需要先测"当前引擎跑到多长开始腐化、丢什么"。

**动作**：
1. 建记忆保持回归集：10 条关键事实，分三类——
   - **早期事实**（第 1–5 轮植入）：如"我的剑名叫'听雨'""我与沈姑娘约定暗号'七月'"；
   - **中期事实**（第 30–60 轮植入）：如"我答应帮老樵夫送柴""沈府后门亥时虚掩"；
   - **数值事实**：某次关键选择的结果（如诗会选择登台）。
2. 写 `scripts/baseline_probe.py`：
   - 用固定 seed 跑 120 轮日常对话（与通关脚本同一驱动）；
   - 在 40 / 80 / 120 轮三个检查点，逐条提问模型（"我的剑叫什么？"），由 judge 判定对错；
   - 输出腐化曲线：每档轮数 × 每类事实的保持率，落盘 `saves/baseline-report.json` + Markdown 报告。
3. 成本估算：120 轮 ≈ 100K–150K token ≈ ¥0.3–0.5（flash 档）。

**验收**：报告产出，明确回答三件事——① 第一处事实丢失出现在哪一档；② 哪类事实最先丢（早期/中期/数值）；③ 是否出现重复调用/答非所问等腐化症状。**基线本身无对错，只有数据**；若 120 轮内零丢失，结论就是"M2a 可以延后"，同样是有效结论。

**实测结果（seed 42，120 回合，报告见 `saves/baseline-report.md`）**：

| 检查点 | 保持率 |
| --- | --- |
| 回合 40（~35K 上下文） | 早期 2/4 |
| 回合 80（~75K） | 早期 2/4 · 中期 1/4 |
| 回合 120（~110K） | 早期 2/4 · 中期 2/4 · 近期 1/2（总体 5/10） |

关键发现（对 M2 规划有直接指导）：

1. **衰减不是纯距离函数**：f2 暗号/f3 家乡从 40 到 120 全部命中，而 f1 剑名/f4 师父在 40 回合就丢了。被后续对话**强化过**的事实存活，一次性植入的事实被埋没——印证书中"上下文只有一半的检索引擎，缺提炼层"的论断。
2. **虚假记忆固化比遗忘更危险**：f5 老樵夫被答成"周老汉"并**跨检查点持续**、f7 被编出"初六成婚"的完整假约定、f10 大慈恩寺被答成"杏林"。模型记不住时会自信地编——对养成游戏是致命的（NPC 会"记得"根本没发生的事）。
3. 协议层面 120 回合零熔断零重试——引擎 Harness 本身稳定，问题全在信息组织。
4. **对 M2 的启示**：纯"压缩历史"不够；优先级应调整为**关键事实显式提炼**（状态栏/NPC 记忆把剑名、约定、承诺提炼成结构化条目常驻注入）→ 压缩（控制成本）→ 语义层校验（防 confabulation）。

### W-E · 真人试玩（用户）

**动作**：用户玩 1–2 局（`uv run python -m game_agent play`），带着体验清单玩，记录问题。

**已收集的反馈（第一轮试玩）与修复**：

| # | 反馈 | 根因 | 修复 |
| --- | --- | --- | --- |
| 1 | 开局没有背景/人物介绍，上来就是选项 | 世界开场白只注入 LLM 上下文、节点 briefing 不展示给玩家 | 开局展示世界介绍；关键抉择前展示【剧情】briefing（TurnView 新增字段） |
| 2 | 交互几轮后自动退出 | 网络/协议异常未被 REPL 捕获 → 裸 traceback 崩溃 | REPL 全局异常捕获 + 崩溃自动存档 saves/crash.json + 错误日志 saves/error.log |
| 3 | 回答速度有待提升 | 整轮生成完才一次性显示，无任何等待反馈 | 流式输出（修复 openai SDK v3 流式对象需手动聚合的 bug）+「生成中…」即时提示 |

**第二轮试玩反馈与修复**：

| # | 反馈 | 根因 | 修复 |
| --- | --- | --- | --- |
| 4 | 背景太少：什么朝代、玩家是谁、要做什么都不清楚，选无可选 | 世界包只有一句话设定，无朝代/身份/目标 | world.yaml 新增 `player_role`/`player_goal` 字段 + 开场白扩写（大盛王朝·游侠·查明师父旧事）；身份与目标**常驻状态栏**（每轮可见的锚点） |
| 5 | 出现整段重复（沈清秋茶肆段落原样复读） | 模型在长上下文下复读已写场景（上下文腐化的一种） | 引擎级**相邻回合重复检测**（相似度 ≥60% 注入反重复提示，要求推进新情节） |
| 6 | 后期剧情混乱，没到那一步剧情就出来了 | 日常对话中模型自发铺陈"诗会"支线，N2 触发后又重写一遍 | 反重复提示 + N2 briefing 加"衔接此前情节续写，勿当作全新场景" |
| 7 | 生成速度"一开始慢后来好多了"，问是否做了什么 | 见下方说明 | 见下方说明 |

**关于速度的说明（反馈 #7）**：没有换模型、没有换提示词。两个原因：① 上一轮已改为**流式输出**——第一个字出来就开始显示，感知速度大幅提升；② **DeepSeek 上下文缓存**——第一轮是冷启动（要完整处理静态前缀+初始上下文，首 token 慢），后续轮次前缀命中缓存（费用 ~1/10、首 token 更快），这正是本书第二章 KV Cache 一章的实战体现：静态前缀设计得好，越聊越快。后续还可优化：思考链长度波动（不同轮次思考深浅不同）无法完全消除。

**体验清单**（供参考，不必逐条）：
1. 节奏：行动/对话/事件的推进节奏是否舒服？等 LLM 响应多久？
2. 选项质量：日常选项是否自然衔接？有没有违和或重复？
3. 剧情连贯：NPC 是否记得之前的事？有没有前后矛盾？
4. 数值反馈：好感/属性变化是否有感知？状态栏信息够不够？
5. 卡壳情况：有没有不知道该干什么、或剧情推不动的时刻？
6. 结局体验：结局到来的时机是否自然？重开是否顺畅？

**产出**：体验问题列表（随意记，格式不限），并进 W-F 的 M2 规划。

### W-F · M2 拆分规划（文档）

**动作**：根据 W-D 基线与 W-E 体验问题，把 M2 拆成三段并落文档（`docs/plan-m2.md`）：

- **M2a（核心）**：剧情摘要压缩 + 记忆回归集复测（压缩后 40/80/120 轮保持率不得低于基线——边界集+保留集）；
- **M2b**：NPC 记忆 + 时间触发事件内容落地 + LLM-Judge 语义层 OOC；
- **M2c（可选）**：Web 前端、多人场景。

**验收**：`docs/plan-m2.md` 成文，每段有独立的完成标准与验收方式。

---

## 2. 依赖与顺序

```
W-A ──→ W-D（需要可复现 seed）
W-B ──→ W-C（引擎小债，独立并行）
         ↘
W-E（用户，可与 W-D 并行进行）
         ↘
W-F（最后，汇总基线与体验数据）
```

- W-A/B/C 由引擎侧完成，除 W-C 的冒烟外全部离线；
- W-D 需要真机（~¥1），可在 W-E（用户试玩）期间跑；
- W-F 等 W-D 报告 + W-E 反馈齐了再动笔。

---

## 3. 检查点

| 检查点 | 内容 | 通过标志 |
| --- | --- | --- |
| ✅ P-A（已达成） | W-A+W-B+W-C 完成 | 测试 95→102 全绿；`--seed` 可复现；时间事件离线验收；自动存档 + `/new` |
| ✅ P-B（已达成） | W-D 完成 | 腐化基线报告落盘（40/80/120 轮保持率；结论见 W-D 小节） |
| ✅ P-C | W-E 完成 | 用户体验问题列表到手 |
| ✅ P-D | W-F 完成 | `docs/plan-m2.md` 定稿（M2a/b/c 各自可独立验收） |

---

## 4. 预算与风险

| 项 | 估计 |
| --- | --- |
| 真机 token（W-D 基线 + W-C 冒烟） | ~150K–250K token ≈ **¥0.5–1.5** |
| 时间 | 引擎侧 2–3 个工作日当量；用户试玩 1–2 小时 |

| 风险 | 对策 |
| --- | --- |
| 网络波动导致基线跑挂 | SDK 已重试 5 次；跑挂则断点续跑（按检查点存档） |
| 120 轮内零腐化（基线"太好"） | 有效结论：M2a 延后，转做 M2b——不浪费 |
| 真人试玩发现体验级大问题 | 问题并入 W-F，可能上调 M2 优先级 |
| 基线提问本身干扰剧情 | 提问走独立"旁白视角"消息（`[记忆检查]`标签），不写入剧情历史 |

---

*计划 v0.1。执行中与设计文档冲突时以实测为准，回写文档。*
