# 侧信道预算修复 · flash 对照线重测报告（2026-09-11）

> 触发改动：`a058b34`（`game_agent/budgets.py` 预算单一真源 + 空响应升级重试推广到
> dedup/reflect/extract）。本报告回答一个问题：**旧 flash 对照线有多少是"预算 bug 拉低"的**，
> 并给出修复后的新对照值。
> 重测范围 = 受预算变更影响的三个侧信道（dedup / reflect / extract）；judge 与 turn / compress
> 未变或经论证无需重测（§3、§4）。

---

## 1. 结论速览

| 项 | 旧值（bug 预算） | 新值（修复后） | 判定 |
| --- | --- | --- | --- |
| dedup 拦截率 | **40-50%**（P1 基线 80%） | **10/10 = 100%** | ✅ 修复即恢复满分——旧结论「14B 100% **完胜** flash 40-50%」**作废**，实为**持平** |
| reflect 产出 | **连续 6 次空响应**、0 洞察 | **2 条洞察**（冒烟矛盾率 0/2） | ⚠️ 空响应类已修；**截断**类未修（§5.1） |
| extract（轨道事实） | flash **3/3**（旧 400 预算、34.8% 调用顶满） | **3/3**；玩家事实桶 14 条 | ✅ 结论不变；新预算下 extract **零顶满**（余量充足） |
| judge | 100% / 0% 误报 | **未重测**（论证见 §3） | — 旧线可用 |

**一句话**：三个受影响侧信道里，dedup 的旧对照线被 bug 压掉了约一半（40-50% → 100%），reflect
从"完全无产出"恢复到"有产出"，extract 本已达标但同样存在大面积顶满（新预算下消失）。
**此次修复会改写一条已验证结论（dedup"完胜"），并暴露两类残留问题（§5）。**

---

## 2. 为什么重测：改前污染取证（免费，历史 usage 全量）

扫描 `reports/usage-*.jsonl` + `saves/usage-*.jsonl` 共 **27 个文件、5796 条**，按 purpose 统计
「completion_tokens 顶满该模块预算」的比例：

| purpose | 模型 | n | 中位 completion | 顶满预算 | 顶满率 |
| --- | --- | --- | --- | --- | --- |
| dedup | deepseek-v4-flash | 456 | **50**（=预算） | 279 | **61.2%** |
| dedup | local-14b | 45 | 3 | 0 | 0.0% |
| reflect | deepseek-v4-flash | 14 | **200**（=预算） | 11 | **78.6%** |
| reflect | local-14b | 3 | 45 | 0 | 0.0% |
| extract | deepseek-v4-flash | 514 | 234 | 179 | **34.8%** |
| extract | local-14b | 31 | 15 | 1 | 3.2% |
| judge | deepseek-v4-flash | 1614 | 183 | 266 | 16.5% |
| judge | local-14b | 279 | 42 | 0 | 0.0% |
| compress | deepseek-v4-flash | 56 | **1999**（≈预算） | 17 | **30.4%** |
| compress | local-14b | 8 | 469 | 0 | 0.0% |

读数：

- **flash 侧大面积顶满，14B 侧≈0** —— 印证机制是"思考链与正文共用 `max_tokens`"（14B 无思考模式）；
- dedup 的旧拦截率 **40-50% ≈ 未被顶满的比例（38.8%）** —— 极可能"能产出正文的那些才判得对"，
  即那个数字基本是 bug 的产物，而不是 flash 的能力值；
- `completion_tokens = 0` 在所有模块都是 **0 次**：顶满时推理链本身也计入 completion，
  所以"顶满"是空响应的**可观测指纹**（这一点对后续监控有用）。

---

## 3. 为什么 judge 不需要重测（一个值得记住的区分）

时序上 judge 基线确实早于修复：基线报告生成于 `2026-09-08 13:30-13:41`，而含升级重试的
`a9064f5` 提交于当天 `20:40`。但 —— **"测量早于修复" ≠ "测量被污染"**，要看 bug 的**偏差方向**：

1. judge 空响应 → `parse_verdict('')` = **通过** = 不拦截 → 空响应只会把**拦截率往下压**，
   绝不可能把 100% 抬高；误报侧同理（空 = 通过，不可能制造误报）。故 **100% / 0% 是保守下界**，
   逻辑上不可能由该 bug 造出；
2. `rounds=3` 多数票（`scripts/judge_sensitivity.py:45`，≥2/3 才算拦截）会吸收单次空响应；
3. 实测佐证：`2026-09-08 13:00-14:00` 窗口 360 次 judge 调用中 31 次顶满 500（8.6%）——
   噪声确实存在，但不足以改变判定方向。

→ **判据：判断一次测量是否被污染，问"该 bug 的偏差方向会不会造出我看到的这个结论"，
而不是只看时间戳先后。**

---

## 4. 重测执行与结果（同命令、同 seed、全程落盘）

| 步骤 | 命令 | 结果 | 成本（空闲价） |
| --- | --- | --- | --- |
| 0 连通性 | `scripts/smoke_llm.py` | ✅ 协议闭环跑通（iterations=1） | ≈¥0.01 |
| 1 dedup | `scripts/dedup_test.py` | ✅ **拦截 10/10 = 100%**（要求 ≥80%） | ¥0.004 |
| 2 reflect | `scripts/reflect_smoke.py` | 洞察 **2 条**、矛盾率 **0/2**（要求 0）✅ | ¥0.174 |
| 3 railed | `scripts/railed_fact_check.py` | 轨道事实 **3/3** ✅；洞察矛盾率 **1/2** ✗（要求 0） | ¥0.462 |
| 3′ | 首次 run **熔断**（`LLMTurnError`，连续 3 次协议失败）→ 按仓库惯例重试一次通过 | 见下 | ≈¥0.05 |

**合计 ≈ ¥0.7**。日志：`reports/retest-budget500-0{0,1,2,3,3b}-*.log`；
usage 追加于 `reports/usage-dedup.jsonl`、`reports/usage-reflect-smoke.jsonl`、`saves/usage-railed.jsonl`；
新 flash 状态档：`saves/railed-fact-check.flash-budget500.json`
（原被跟踪的 14B 档已 `git checkout` 还原，两者并存，沿用 `.flash0908` 后缀惯例）。

**关于步骤 3′ 的熔断**：turn 路径本次**未改**（`MAX_OUTPUT_TOKENS=2048` 仅搬迁常量、值不变），
熔断属该模型瞬时协议失败，重试即恢复 —— 与预算改动无关；但按测量纪律记录在案。

### 新 flash 档关键数字（`railed-fact-check.flash-budget500.json`）

- 第 10 天、21 回合、结局「江湖独行」；轨道事实 3/3（听雨 / 陆 / 江南水乡）；
- 玩家事实桶 **14 条**（身份、剑名、师承、约定、线索链俱全）；NPC 记忆 15 条；洞察 2 条。

---

## 5. 本次修复**未覆盖**的残留问题（建议立项）

逐调用取证（本次窗口内）：

| purpose | 调用数 | 顶满新基础预算(500) | 顶满升级预算(2000) |
| --- | --- | --- | --- |
| dedup | 59 | 10（16.9%，旧 61.2%） | **0** |
| reflect | 6 | **5（83.3%）** | 0 |
| extract | 16 | **0**（旧 34.8%） | 0 |

1. **截断（非空）不触发升级重试** —— 本次修的是"空"，但 reflect 6 次调用里 5 次顶满 500，
   产出的是**非空而被截断**的正文：冒烟与 railed 两次都出现"洞察断在半句、来源编号丢失"
   （如「…关系正走向隐」）。根因：`LLMClient.complete()` 只返回正文、丢弃 `finish_reason`，
   而重试判据只认"空"。**建议**：把 `finish_reason` 透出到侧信道，`length` 同样走升级重试
   （或按模块调高基础预算）。
2. **judge 的升级预算 2000 也会被顶满** —— railed 中 12 次 judge 调用呈
   `500, 2000, 500, 2000 …` 成对出现，其中 **4 次顶满 2000**；顶满且为空时仍走
   `parse_verdict('')` = 「通过」静默放行。**建议**：升级后仍空时标记「未知」而非默认通过
   （属产品语义决策，需与门禁判据一起定）。
3. **reflect 基础预算 500 偏紧**（83% 调用顶满）→ 建议 1000~1500，或纳入第 1 条的 length 重试。
4. **compress 未纳入重试是刻意边界**（其空响应=保留原历史的 安全降级），但取证显示
   **30.4% 的 flash compress 调用顶满 2000** → Phase 1 的 compress 训练数据靠 flash 补标，
   **补标预算必须 >2000**（对应 retro §10.4 第一条）。

---

## 6. 未做项（明确记录，避免误以为已覆盖）

- **`memory_regression` 121 回合未重跑**。理由：① extract 的问题已由 railed 3/3 + "零顶满"
  回答；② 历史档属 M2a 时代（`compress_threshold=0`），与当前配置不可比，重跑得到的是**新基线**
  而非"同一实验的重测"；③ 成本/时长（≈¥2、约 20 分钟）对当前决策边际价值低。
  需要"期望事实召回率"曲线时再跑。

---

## 7. 对既有文档的连带影响（已同步修改）

- `docs/local14b-p012-retro.md` §1 速览 / §6.2：「14B dedup 100% **完胜** flash 40-50%」
  → 改为**两者均 100%、持平**；dedup 的处置「不训」不变，但**理由从"完胜"变为"无差距"**。
- `reports/local14b-p0-20260911.md` §1 / §4 勘误块：把"40-50% 系预算 bug 拉低"补上实测值 100%。
- `docs/plan-local-14b.md`：§10 滚动复盘追加本次重测条目。
