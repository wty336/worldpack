- 2026-09-13 WRITE 出库 69 条；此后每次打开（读取用于决策）须在此追加一行计数。
- 2026-09-13 READ 打开 1 次：Task 11 验收核对 manifest/sha256 与成本回填（未用于模型选择）。

- 2026-09-13 VOID 作废 2026-09-13 那次写入（69 条：compress 468a22974a1d905b / extract 03b7f4734e471638 / judge ef43477dc0bec784）：发现①`card_id` 层内不唯一 = **尺子自身缺陷**（决策 14 例外条款允许一次修复）—— id 不能当主键，质检剔除按 id 连坐（实剔 3 报 1）。作废该次结果、递增打开计数、修复 diff 入库（提交 ca710d4）。旧 tag eval-route-a-20260912 保留为作废留痕。
- 2026-09-13 WRITE 出库 88 条；此后每次打开（读取用于决策）须在此追加一行计数。
- 2026-09-13 VOID（第 2 次，决策 14 例外条款）作废上一次写入（88 条：compress 54d31ee5ad0d0aaa / extract 4c6490c1cf71ba19 / judge 64f292a8e7629d2b）：发现⑦「名字池不分语义槽」= 标签级缺陷（preserve_points 声称的内容在材料里不成立，判官如实给 保真=0）→ 冻结层不可用于其用途，修复 diff 见提交 13ced55。旧 tag eval-route-a-20260913 保留为作废留痕；重写后另打新 tag。
- 2026-09-13 WRITE 出库 88 条；此后每次打开（读取用于决策）须在此追加一行计数。
- 2026-09-13 VOID（第 3 次，决策 14 例外条款）作废上一次写入（88 条：compress 8185b62870f126b8 / extract 7467cd543ca61a41 / judge 4cb5b8fb81e7885c）：该批是「标签门禁前置到生成时」（发现⑧ 的根治，提交 5636464）**之前**的产物 —— judge 批判「问题类型不成立」达 41%（36/87），冻结层不可用于其用途。旧 tag eval-route-a-20260913b 保留为作废留痕；重写后另打新 tag。
- 2026-09-13 WRITE 出库 77 条；此后每次打开（读取用于决策）须在此追加一行计数。
- 2026-09-16 VOID（第 4 次，决策 14 例外条款 + 决策 32）作废上一次写入（77 条：compress 25a80b69a9617947 / extract 9c15aba0fa2847ce / judge 2e5b8dd237de373c）：按决策 32「生产批只出 train+dev，**eval 层单独跑一次**再冻结打 tag」重写整层；且本次重写同时带上两处已修复的缺陷 —— 发现⑩（元叙述/标签泄漏）、**决策 36（confab 材料形状泄露类别：修前「材料无事实段 ⇒ 虚构事实」100% 命中且零误报）**。旧 tag `eval-route-a-20260913c` 保留为作废留痕；整层写完后另打新 tag。
- 2026-09-16 WRITE 出库 198 条（extract 第 1 段，**分阶段构建**：先产 200 张做质量核验）；此后每次打开（读取用于决策）须在此追加一行计数。
  - ⚠️ **分阶段构建注记**：本层会出现**多条 WRITE 行**（每段发布一次）。`manifest.frozen` 现与 `complete` 绑定 —— **只有最终整层达标的写入才是冻结态**，此前的中间态一律 `frozen=false`（修前它们会被写成 `frozen=true`＋`complete=true`，而只含 198/1000 条 ⇒ 读的人会把半成品当成那杆"尺子"）。该口径由守卫 `test_eval_frozen_requires_the_declared_targets_to_be_met` 钉住。
  - 本段核验：198 条、0 丢弃（2 张标签门禁）、标签自检 32 抽检 **0 不一致**；独立重验（逐条重生成卡面）**零偏差**；**留出轴值产出正常** —— 民国谍战 38 条（金额词「三十块大洋」在位 25、穿帮 0）、书信体 42 条（真的是书信体，且负例确实没引入新事实）。
- 2026-09-18 WRITE 出库 2321 条；此后每次打开（读取用于决策）须在此追加一行计数。
